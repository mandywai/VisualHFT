<UserControl x:Class="VisualHFT.Studies.DataRecorder.UserControls.PluginSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="760" d:DesignWidth="520">
    <Grid Margin="10">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Label Content="Exchange / Provider"/>
                <ComboBox Margin="0,5"
                          ItemsSource="{Binding Providers}"
                          SelectedItem="{Binding SelectedProvider, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                          DisplayMemberPath="ProviderName"
                          FontSize="16" />

                <Label Content="Symbol"/>
                <ComboBox Margin="0,5"
                          ItemsSource="{Binding Symbols}"
                          SelectedItem="{Binding SelectedSymbol, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                          FontSize="16" />

                <Label Content="Capture Mode"/>
                <ComboBox Margin="0,5"
                          ItemsSource="{Binding CaptureModes}"
                          DisplayMemberPath="Item1"
                          SelectedValuePath="Item2"
                          SelectedValue="{Binding CaptureModeSelection, UpdateSourceTrigger=PropertyChanged}"
                          FontSize="16" />

                <Label Content="Fixed Interval"/>
                <ComboBox Margin="0,5"
                          ItemsSource="{Binding AggregationLevels}"
                          DisplayMemberPath="Item1"
                          SelectedValuePath="Item2"
                          SelectedValue="{Binding AggregationLevelSelection, UpdateSourceTrigger=PropertyChanged}"
                          IsEnabled="{Binding IsFixedInterval}"
                          FontSize="16" />

                <Label Content="Output Folder"/>
                <DockPanel Margin="0,5">
                    <Button Content="Browse"
                            Command="{Binding BrowseFolderCommand}"
                            Width="90"
                            DockPanel.Dock="Right"
                            Margin="8,0,0,0" />
                    <TextBox Text="{Binding OutputFolder, UpdateSourceTrigger=PropertyChanged}" />
                </DockPanel>

                <CheckBox Content="Run until manually stopped"
                          IsChecked="{Binding RunIndefinitely}"
                          Margin="0,10,0,0" />

                <Label Content="Duration (minutes)"/>
                <TextBox Margin="0,5"
                         Text="{Binding DurationMinutes, UpdateSourceTrigger=PropertyChanged}"
                         IsEnabled="{Binding IsDurationEnabled}" />

                <Separator Margin="0,16,0,12" />
                <Label Content="Market Fields To Store" FontWeight="Bold"/>
                <ItemsControl ItemsSource="{Binding MarketFieldOptions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Label}" IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Margin="0,2" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Separator Margin="0,16,0,12" />
                <Label Content="Study Metrics To Store" FontWeight="Bold"/>
                <TextBlock Text="Any selected study will be written as a live stream in update mode and included as the latest value in interval mode."
                           TextWrapping="Wrap"
                           Margin="0,0,0,6"/>
                <ItemsControl ItemsSource="{Binding StudyOptions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Label}" IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Margin="0,2" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Label Content="{Binding ValidationMessage}" Foreground="Red" Margin="0,0,16,0" VerticalAlignment="Center" />
                    <Button Content="OK" Command="{Binding OkCommand}" Margin="0,0,5,0" Width="80" />
                    <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
