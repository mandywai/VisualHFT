<UserControl x:Class="VisualHFT.Studies.VPINMulti.UserControls.PluginSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="640"
             d:DesignWidth="720">
    <Grid Margin="10">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Label Content="Provider" />
                <ComboBox Margin="0,5"
                          ItemsSource="{Binding Providers}"
                          SelectedItem="{Binding SelectedProvider, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                          DisplayMemberPath="ProviderName"
                          FontSize="16" />

                <Label Content="Symbol" />
                <ComboBox Margin="0,5"
                          ItemsSource="{Binding Symbols}"
                          SelectedItem="{Binding SelectedSymbol, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                          FontSize="16" />

                <Separator Margin="0,12,0,12" />
                <DockPanel Margin="0,0,0,6">
                    <Label Content="VPIN Profiles" FontWeight="Bold" DockPanel.Dock="Left" />
                    <Button Content="Add Profile"
                            Command="{Binding AddProfileCommand}"
                            Width="110"
                            DockPanel.Dock="Right" />
                </DockPanel>

                <ItemsControl ItemsSource="{Binding Profiles}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#444" BorderThickness="1" Padding="8" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*" />
                                        <ColumnDefinition Width="1.5*" />
                                        <ColumnDefinition Width="1.5*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="Profile Name" Grid.Row="0" Grid.Column="0" Margin="0,0,8,4" />
                                    <TextBlock Text="Bucket Volume Size" Grid.Row="0" Grid.Column="1" Margin="0,0,8,4" />
                                    <TextBlock Text="Number of Buckets" Grid.Row="0" Grid.Column="2" Margin="0,0,8,4" />

                                    <TextBox Grid.Row="1" Grid.Column="0" Margin="0,0,8,0"
                                             Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,8,0"
                                             Text="{Binding BucketVolumeSize, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Grid.Row="1" Grid.Column="2" Margin="0,0,8,0"
                                             Text="{Binding NumberOfBuckets, UpdateSourceTrigger=PropertyChanged}" />
                                    <Button Grid.Row="1" Grid.Column="3"
                                            Content="Remove"
                                            Width="80"
                                            Command="{Binding DataContext.RemoveProfileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                    <Label Content="{Binding ValidationMessage}" Foreground="Red" Margin="0,0,16,0" VerticalAlignment="Center" />
                    <Button Content="OK" Command="{Binding OkCommand}" Margin="0,0,5,0" Width="80" />
                    <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
