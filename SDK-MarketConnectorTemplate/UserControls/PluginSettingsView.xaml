<UserControl x:Class="MarketConnector.Template.UserControls.PluginSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10" Orientation="Vertical">

            <!-- Provider identity -->
            <Label Content="Provider ID" Margin="0,0,0,0"/>
            <TextBox Text="{Binding ProviderId, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

            <Label Content="Provider Name" Margin="0,10,0,0"/>
            <TextBox Text="{Binding ProviderName, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

            <!-- API credentials -->
            <TextBlock Text="API Key (optional — only required for authenticated streams)"
                       FontWeight="Bold" Margin="0,20,0,5"/>
            <TextBox Text="{Binding ApiKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

            <Label Content="API Secret" Margin="0,10,0,0"/>
            <TextBox Text="{Binding ApiSecret, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

            <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="Gray">
                Credentials are stored locally on this machine. Leave empty if your exchange exposes order-book and trade
                data through public endpoints.
            </TextBlock>

            <!-- Symbols -->
            <Label Content="Symbols (comma-separated)" Margin="0,20,0,0"/>
            <TextBox Margin="0,5"
                     Text="{Binding SymbolsText, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Format per entry: RAW or RAW(NORMALIZED). Example: BTCUSDT(BTC/USD),ETHUSDT(ETH/USD)">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Style.Triggers>
                            <Trigger Property="Validation.HasError" Value="true">
                                <Setter Property="Background" Value="LightPink"/>
                                <Setter Property="ToolTip"
                                        Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>

            <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="Gray">
                Use parentheses to map an exchange-native symbol to the normalized symbol VisualHFT will display.
                For example, BTCUSDT(BTC/USD) tells the plug-in that the raw symbol BTCUSDT corresponds to BTC/USD.
            </TextBlock>

            <!-- Depth levels -->
            <Label Content="Depth Levels" Margin="0,20,0,0"/>
            <TextBox Text="{Binding DepthLevels, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

            <!-- Actions -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Label Content="{Binding ValidationMessage}" Foreground="Red" Margin="0,0,20,0"/>
                <Button Content="OK"     Command="{Binding OkCommand}"     Margin="0,0,5,0" Width="80"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
